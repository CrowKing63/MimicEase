> **[MimicEase 사양서 — 07/11]** 독립 작업 가능 단위
> **프로젝트**: Google Project GameFace(Android) 기반 표정 인식 안드로이드 접근성 앱
> **스택**: Kotlin + Jetpack Compose, API 29+, MediaPipe 온디바이스 ML
> **전체 목차**: [`docs/00_INDEX.md`](./00_INDEX.md)

---

# 07. 화면 설계 (UI/UX Specification)

## 7.1 네비게이션 구조

`NavigationBar` (Bottom Navigation) + `NavHost` (Compose Navigation)

```
MainActivity
└── NavHost
    ├── OnboardingScreen      (onboardingCompleted=false일 때 시작)
    └── MainScreen
        ├── BottomNavBar
        │   ├── Tab 1: 홈 (Home)          icon: Icons.Default.Home
        │   ├── Tab 2: 테스트 (Test)       icon: Icons.Default.Face
        │   ├── Tab 3: 프로필 (Profiles)   icon: Icons.Default.Person
        │   └── Tab 4: 설정 (Settings)    icon: Icons.Default.Settings
        └── NavDestinations
            ├── HomeScreen
            ├── ExpressionTestScreen
            ├── ProfileListScreen
            │   ├── ProfileEditScreen
            │   └── TriggerListScreen (특정 프로필)
            │       ├── TriggerEditScreen
            │       │   ├── ActionPickerBottomSheet
            │       │   ├── CoordinatePickerScreen
            │       │   └── AppPickerScreen
            │       └── TriggerTestOverlay (5초 테스트 오버레이)
            └── SettingsScreen
```

---

## 7.2 홈 화면 (HomeScreen)

**역할**: 서비스 상태 한눈에 파악 + 빠른 조작

```
┌─────────────────────────────────────────────┐
│  MimicEase                    ⚙️             │  ← TopAppBar
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  서비스 상태                          │   │  ← 상태 카드 (ElevatedCard)
│  │                                     │   │
│  │   ● 표정 감지 중                     │   │  ← 상태에 따라 색상 변경
│  │   FPS: 28    추론시간: 32ms          │   │  ← 개발자 모드에서만 표시
│  │                                     │   │
│  │         [ 일시정지 ]                 │   │  ← 큰 토글 버튼
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  현재 프로필                          │   │  ← 프로필 카드
│  │  😊  기본 프로필   트리거 4개 활성    │   │
│  │                   [프로필 변경 ›]    │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  빠른 트리거                                │  ← 섹션 헤더
│  ┌──────────────────────────────────────┐  │
│  │ 오른쪽 윙크   →  뒤로가기    [●]    │  │  ← 트리거 카드 (활성화 토글 포함)
│  │ 왼쪽 윙크     →  홈 버튼     [●]    │  │
│  │ 입 벌리기     →  스크롤 위   [●]    │  │
│  │ 눈썹 올리기   →  최근 앱     [○]    │  │  ← 비활성화 상태
│  │                    [전체 보기 →]    │  │
│  └──────────────────────────────────────┘  │
│                                             │
├─────────────────────────────────────────────┤
│  🏠 홈   😀 테스트   👤 프로필   ⚙️ 설정   │  ← BottomNavBar
└─────────────────────────────────────────────┘
```

**HomeUiState**:
```kotlin
data class HomeUiState(
    val isServiceRunning: Boolean = false,
    val isPaused: Boolean = false,
    val currentFps: Int = 0,
    val inferenceTimeMs: Long = 0,
    val activeProfile: Profile? = null,
    val quickTriggers: List<Trigger> = emptyList(),  // 최대 5개
    val isDeveloperMode: Boolean = false
)
```

---

## 7.3 표정 테스트 화면 (ExpressionTestScreen)

(상세 레이아웃은 `04_expression_test.md` 참조)

**핵심 Composable 구조**:
```kotlin
@Composable
fun ExpressionTestScreen(viewModel: ExpressionTestViewModel) {
    val uiState by viewModel.uiState.collectAsState()

    Column {
        // 1. 카메라 프리뷰 (CameraPreview Composable)
        CameraPreviewSection(modifier = Modifier.fillMaxWidth().aspectRatio(1f))

        // 2. 카테고리 탭
        ScrollableTabRow(selectedTabIndex = uiState.selectedCategory.ordinal) { ... }

        // 3. 블렌드쉐이프 목록
        LazyColumn {
            items(uiState.filteredBlendShapes) { (name, value) ->
                BlendShapeRow(name = name, value = value, onExpand = { ... })
            }
        }

        // 4. FAB
        ExtendedFloatingActionButton(
            text = { Text("이 표정으로 트리거 만들기") },
            icon = { Icon(Icons.Default.Add, null) },
            onClick = { navController.navigate("triggerEdit/${uiState.topExpressions.firstOrNull()?.first}") }
        )
    }
}
```

---

## 7.4 프로필 목록 화면 (ProfileListScreen)

```
┌─────────────────────────────────────────────┐
│  ← 프로필                           [+]     │  ← TopAppBar
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  😊 기본 프로필              [●] [›] │   │  ← 활성화된 프로필 (강조)
│  │  트리거 4개                          │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  🎮 게임용                   [○] [›] │   │
│  │  트리거 2개                          │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  📺 영상 시청                [○] [›] │   │
│  │  트리거 3개                          │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  (스와이프 삭제 제스처 지원)                  │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 7.5 트리거 목록 화면 (TriggerListScreen)

```
┌─────────────────────────────────────────────┐
│  ← 😊 기본 프로필                  [프로필 편집]│
├─────────────────────────────────────────────┤
│  감도: ●────────○  1.0x    쿨다운: 300ms    │  ← 프로필 설정 요약
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ 👁 오른쪽 윙크   →   뒤로가기  [●][›]│   │
│  │    eyeBlinkRight  0.6  홀드 300ms   │   │
│  └─────────────────────────────────────┘   │
│  ┌─────────────────────────────────────┐   │
│  │ 👁 왼쪽 윙크    →   홈 버튼   [●][›]│   │
│  │    eyeBlinkLeft   0.6  홀드 300ms   │   │
│  └─────────────────────────────────────┘   │
│  ┌─────────────────────────────────────┐   │
│  │ 😮 입 벌리기   →   스크롤 위  [○][›]│   │
│  │    jawOpen  0.5  홀드 200ms         │   │
│  └─────────────────────────────────────┘   │
│                                             │
│           [+ 트리거 추가]                   │  ← FAB
└─────────────────────────────────────────────┘
```

---

## 7.6 설정 화면 (SettingsScreen)

```
┌─────────────────────────────────────────────┐
│  설정                                        │
├─────────────────────────────────────────────┤
│  감지 설정                                   │  ← 섹션 헤더
│  ┌───────────────────────────────────────┐  │
│  │ 표정 평활화 (EMA)                      │  │
│  │ [느림 ●───────○ 빠름]   0.5           │  │
│  ├───────────────────────────────────────┤  │
│  │ 연속 확정 프레임                        │  │
│  │ [1 ●────────○ 10]   3프레임           │  │
│  ├───────────────────────────────────────┤  │
│  │ 카메라 방향                            │  │
│  │ ● 전면    ○ 후면                       │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  알림 설정                                   │
│  ┌───────────────────────────────────────┐  │
│  │ 포그라운드 알림 표시        [●]        │  │
│  ├───────────────────────────────────────┤  │
│  │ 알림 탭 동작               앱 열기 ›  │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  시스템                                      │
│  ┌───────────────────────────────────────┐  │
│  │ 접근성 서비스     활성화됨 ✓  [설정 →]│  │
│  ├───────────────────────────────────────┤  │
│  │ 카메라 권한       허용됨 ✓            │  │
│  ├───────────────────────────────────────┤  │
│  │ 배터리 최적화 제외          [○]       │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  기타                                        │
│  ┌───────────────────────────────────────┐  │
│  │ 개발자 모드                 [○]        │  │
│  ├───────────────────────────────────────┤  │
│  │ 도움말 / 튜토리얼 다시 보기      [›]  │  │
│  ├───────────────────────────────────────┤  │
│  │ 버전                          1.0.0   │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

**SettingsUiState**:
```kotlin
data class SettingsUiState(
    val emaAlpha: Float = 0.5f,
    val consecutiveFrames: Int = 3,
    val cameraFacing: Int = CameraSelector.LENS_FACING_FRONT,
    val showNotification: Boolean = true,
    val notificationTapAction: NotificationTapAction = NotificationTapAction.OPEN_APP,
    val isAccessibilityEnabled: Boolean = false,
    val isCameraGranted: Boolean = false,
    val isBatteryOptimizationExcluded: Boolean = false,
    val isDeveloperMode: Boolean = false,
    val appVersion: String = ""
)
```

---

## 7.7 포그라운드 서비스 알림

```
┌──────────────────────────────────────────────┐
│  🎭 MimicEase                                │
│  표정 감지 중 — 😊 기본 프로필               │
│                                              │
│  [일시정지]                    [앱 열기]     │
└──────────────────────────────────────────────┘
```

```kotlin
fun buildNotification(profile: Profile?, isPaused: Boolean): Notification {
    val pauseAction = NotificationCompat.Action(
        R.drawable.ic_pause,
        if (isPaused) "재개" else "일시정지",
        PendingIntent.getService(context, 0, pauseIntent, PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE)
    )
    val openAction = NotificationCompat.Action(
        R.drawable.ic_open,
        "앱 열기",
        PendingIntent.getActivity(context, 0, openIntent, PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE)
    )

    return NotificationCompat.Builder(context, CHANNEL_ID)
        .setSmallIcon(R.drawable.ic_mimic)
        .setContentTitle("MimicEase")
        .setContentText(if (isPaused) "일시정지됨" else "표정 감지 중 — ${profile?.name ?: "프로필 없음"}")
        .setPriority(NotificationCompat.PRIORITY_LOW)
        .setOngoing(true)
        .addAction(pauseAction)
        .addAction(openAction)
        .build()
}
```

---

## 7.8 공통 디자인 토큰

| 항목 | 값 |
|------|-----|
| Primary Color | Material 3 `dynamicColorScheme` 또는 기본 Blue (#1976D2) |
| 활성 상태 색상 | Green (#4CAF50) |
| 비활성 상태 색상 | Gray (#9E9E9E) |
| 카드 모서리 | `RoundedCornerShape(16.dp)` |
| 카드 elevation | `ElevatedCard` 기본값 (2dp) |
| 기본 패딩 | `16.dp` |
| 항목 간격 | `8.dp` |
| 폰트 | Material 3 기본 (`Typography`) |
